# .env.test - 테스트 환경 설정
NODE_ENV=test

# 같은 MySQL 인스턴스, 다른 데이터베이스
DATABASE_URL="mysql://root:password@localhost:3306/calendar_test"

# JWT 설정
JWT_SECRET=test-jwt-secret-key
JWT_EXPIRES_IN=1h

# 로깅 설정 (테스트에서는 최소화)
LOG_LEVEL=error
PRISMA_LOG_LEVEL=error

# 기타 설정
KAFKA_ENABLED=false
PORT=3001
